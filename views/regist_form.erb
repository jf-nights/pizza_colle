<div id="regist_form">
  <h2>着任式でーす</h2>
  <form action="/regist_user" method="post" class="form-horizontal">
    <fieldset>
      <legend>登録</legend>
      <div class="form-group">
        <% if @duplicate == true %>
          <p>ソビエトロシアでは登録が既にその名前されている!</p>
        <% end %>
        <label class="col-sm-2" for="user_name">名前</label>
        <div class="co-sm-10">
          <input type="text" class="form-control" name="user_name" pattern="^[0-9A-Za-z]+$" placeholder="user name" required>
        </div>
      </div>
      <div class="form-group">
        <label class="col-sm-2" for="pass">パスワード</label>
        <div class="co-sm-10">
          <input type="password" class="form-control" name="password" placeholder="password" required>
        </div>
      </div>
      <button type="submit" class="btn btn-primary">登録</button>
    </fieldset>
  </form>
</div>
